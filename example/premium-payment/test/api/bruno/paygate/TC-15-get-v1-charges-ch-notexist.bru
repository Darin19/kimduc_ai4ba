meta {
  name: TC-15 GET /v1/charges/ch_notexist
  type: http
  seq: 15
}

get {
  url: {{PAYGATE_BASE}}/v1/charges/ch_notexist
  auth: bearer
}

auth:bearer {
  token: {{process.env.PAYGATE_KEY}}
}

assert {
  res.status: eq 404
  res.body.error.code: eq resource_missing
}
