meta {
  name: TC-05 GET /v1/charges?limit=2
  type: http
  seq: 5
}

get {
  url: {{PAYGATE_BASE}}/v1/charges?limit=2
  auth: bearer
}

auth:bearer {
  token: {{process.env.PAYGATE_KEY}}
}

assert {
  res.status: eq 200
  res.body.object: eq list
}
